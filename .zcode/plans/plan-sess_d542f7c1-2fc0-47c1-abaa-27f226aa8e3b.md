# P0-P2 安全修复与优化计划（共 10 项）n## P0：严重n### P0-1：历史记录用户隔离（IDOR 修复）n**问题**：`ReviewHistory` 无 `user_id` 字段，任何已认证用户可查看/删除所有记录。n**修改文件与步骤**：nn1. **`src/geo_review/history/models.py`** — 添加 `user_id` 列：n   - 在 `ReviewHistory` 类中（`submitted_by` 列之后，约第 47 行）添加：n     ```pythonn     user_id = Column(String(36), nullable=True, index=True)n     ```n   - 由于项目使用 `create_all`（不存在则建表），nullable 列对已有数据库安全。nn2. **`src/geo_review/history/service.py`** — 修改查询和保存：n   - `save_review()` 签名添加 `user_id: Optional[str] = None` 参数n   - 第 86 行 `submitted_by=None` 旁边添加 `user_id=user_id`n   - `list_reviews()` 添加 `user_id` 和 `is_admin` 参数：n     - 非 admin 用户：在 where 条件中添加 `ReviewHistory.user_id == user_id`（同时应用到 count 查询和 data 查询，修复现有 count 查询缺少 start_date/end_date/batch_id 过滤的 bug）n     - admin 用户：不添加过滤（可看全部）n   - `get_review()` / `get_review_with_issues()`：添加 `user_id` 和 `is_admin` 参数，非 admin 时额外过滤 `user_id`n   - `delete_review()` / `batch_delete()`：添加 `user_id` 和 `is_admin` 参数，非 admin 时额外过滤 `user_id`（防止越权删除）n   - `get_statistics()`：添加 `user_id` 和 `is_admin` 参数n   - `get_batch_results()`：添加 `user_id` 和 `is_admin` 参数n   - `update_human_review()`：添加 `user_id` 和 `is_admin` 参数n   - `backfill_inferred_names()`：不改（内部维护，不受用户隔离影响）nn3. **`src/geo_review/api/routers/history.py`** — 传递用户信息：n   - 每个端点从 `current_user` 提取 `user_id` 和 `role`n   - 传递 `user_id=current_user.id, is_admin=(current_user.role == 